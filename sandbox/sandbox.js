// Sandbox file for quick tests
// Save and then press Ctrl + Alt + N to run in VS Code (having installed node and the Code Runner extension)

for (let index = 0; index < 7; index++) {
    console.log(index);
}
console.log(Math.random());